import React from 'react';
import {Icon} from '@ui-kitten/components';

export const HomeIcon = (props) => (
  <Icon {...props} name="home" pack="assets" />
);
export const JournalIcon = (props) => (
  <Icon {...props} name="journal" pack="assets" />
);
export const LogIcon = (props) => <Icon {...props} name="log" pack="assets" />;

export const SettingIcon = (props) => (
  <Icon {...props} name="setting" pack="assets" />
);

export const AirIcon = (props) => <Icon {...props} name="air" pack="assets" />;
